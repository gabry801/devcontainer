{
  "name": "Alpine DevContainer",
  "image": "alpine:latest",
  "postCreateCommand": "echo 'Hello from DevContainer' > /workspace/hello.txt",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/sh"
  }
}
